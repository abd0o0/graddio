open vs code and in the terminal write " pip install -r requirements.txt "
with out the double quetations.

you need to install ffmpeg this one is to save the voice you are recording to your files
to install it you can go to there website or run the command in your power shell 
to Install Chocolatey type:
"Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.WebClient]::new().DownloadString('https://chocolatey.org/install.ps1') | i"

Once Chocolatey is installed, run the command 
choco install ffmpeg
You can then add the ffmpeg executable to your system's PATH environment variable to be able to run ffmpeg commands from any Command Prompt or PowerShell window.

if everything went fine ;=) then run the project and you will get a local URL open the url and try it 




or you can just run the script befor that  just add the ffmpeg executable to your system's PATH environment variable